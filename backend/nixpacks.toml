[phases.setup]
nixPkgs = ["...", "gcc", "gfortran"]
aptPkgs = ["libopenblas-dev", "liblapack-dev", "gfortran", "chromium", "chromium-driver"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]
